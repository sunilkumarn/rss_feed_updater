source 'https://rubygems.org'

# Google APIs client library
gem 'google-api-client', '~> 0.53.0'

# XML manipulation
gem 'rexml', '~> 3.2.5'

# To use `ffprobe` command for audio duration (requires ffmpeg installed on system)
gem 'tempfile'

# HTTP requests
gem 'open-uri'

gem 'nokogiri'
